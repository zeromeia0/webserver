#pragma once

#include "main.hpp"

enum CONNECTION_TYPE {
	SERVER,
	CLIENT,
	CGI_IN,
	CGI_OUT,
};

enum CGI_STATE {
	DEFAULT,
	ONGOING,
	DONE,
};

enum CLIENT_STATE {
	READING_HEADERS,
	READING_PAYLOAD,
	COMPLETED,
};

enum TRANSFER_TYPE {
	CONTENT,
	CHUNKED,
};

enum RE_TYPE {
	REQ,
	RES,
};

enum RE_METHOD {
	GET,
	POST,
	DELETE,
	HEAD,
};
